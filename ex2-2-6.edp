border b1(t=0, 2*pi){ x=cos(t); y=sin(t); label=1; }
real hmax = .1;
int  nborder = 2*pi / hmax;
mesh Th = buildmesh(b1(nborder));
fespace Vh(Th, P1);
func f = exp(-x^2 - y^2);
Vh fh = f;

plot(Th, fh);